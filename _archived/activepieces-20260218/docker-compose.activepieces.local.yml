services:
  activepieces:
    image: activepieces-bcgpt:local
    build:
      context: ./activepieces
      dockerfile: Dockerfile
    environment:
      AP_FRONTEND_URL: "${AP_FRONTEND_URL:-http://localhost:8080}"
    ports:
      - "8080:80"
