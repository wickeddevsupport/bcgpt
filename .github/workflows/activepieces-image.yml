name: Build Activepieces Image

# Disabled - deploying directly on server
# on:
#   workflow_dispatch:
#   push:
#     branches:
#       - main
#     paths:
#       - "activepieces/**"
#       - ".github/workflows/activepieces-image.yml"

on:
  workflow_dispatch:
    inputs:
      disabled:
        description: 'Workflow disabled - use manual deployment'
        required: false
        default: 'true'

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/wickeddevsupport/activepieces-bcgpt
          tags: |
            type=raw,value=latest
            type=sha,format=short

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: ./activepieces
          file: ./activepieces/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
